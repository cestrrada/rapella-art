---
label: Art
hide_body: false
fields:
- name: layout
  type: select
  default: single
  config:
    required: true
    options:
    - single
    - page
    - collection
    - default
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Layout
  hidden: true
- name: title
  type: text
  config:
    required: true
  label: Title
- name: date
  type: datetime
  label: Date
  description: ''
  config:
    required: false
    date_format: MM/YYYY
    time_format: h:mm A
    display_utc: false
- name: medium
  type: select
  default: ''
  config:
    required: false
    options:
    - Digital
    - Fine
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Medium
- name: categories
  type: list
  config:
    use_select: false
    min: 
    max: 
    source:
      type: documents
  label: categories
  description: Add and reorder your piece's categories.
- name: keywords
  type: tag_list
  default: []
  label: Keywords
  description: A list of specific keywords for Google search to use (e.g. art, horse,
    people, etc.)
- name: featured_image
  type: file
  config:
    maxSize: '5'
  label: Featured Image
  description: Upload a single image. If this is part of a collection, specify the
    collection and upload the other images separately.
- name: slug
  type: text
  config:
    required: true
  label: slug
  description: This is usually the same thing as the title but in all lowercase. For
    example, if the title is My New Painting, the slug would be my-new-painting.
pages:
- _posts/2019-10-01-test-post.md
